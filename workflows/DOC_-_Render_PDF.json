{
  "name": "DOC - Render PDF",
  "nodes": [
    {
      "parameters": {
        "content": "## DOC - Render PDF\n\nGénère un PDF à partir d'un **template** et de **données structurées**.\nPoint d'entrée commun à tous les clients du parc.\n\n**POST** `/webhook/render-pdf` → répond **synchronement** avec le PDF binaire.\n\n`/webhook/torah-pdf` est un **alias historique** (même traitement), conservé\npour le plugin torah. Utiliser `render-pdf` pour tout nouveau client.\n\n### Rôle : PROXY MINCE\n\nLa mise en page vit dans **gotenberg.api** (`webs.local:3100`), avec ses templates.\nn8n valide, relaie et renvoie — **aucun binaire n'est construit ici**.\n\n> Historique : la version precedente assemblait le multipart dans un node Code.\n> `helpers.httpRequest` altere les corps `Buffer` (memes octets : HTTP 200 via curl,\n> 400 via n8n), saturant un coeur 24-35 min sur les gros documents.\n\n### Entrée\n```json\n{ \"template\": \"bavli-visavis\",\n  \"data\": { ... },\n  \"filename\": \"document.pdf\" }\n```\n`template` est **requis** sur `/render-pdf`.\nSur l'alias `/torah-pdf`, il vaut `bavli-visavis` par défaut.\n\nTemplates et schémas : `GET http://webs.local:3100/templates`\n\n### Format\n`format` : `\"pdf\"` (défaut) ou `\"epub\"` → `/render/{format}` du service. Format inconnu → 400 `UNSUPPORTED_OUTPUT`, **jamais de repli** sur du PDF. Gabarit qui ne déclare pas ce format → 400 `UNSUPPORTED_OUTPUT` du service, relayé.\n\n### Catalogue\n**GET** `/webhook/pdf-templates` → la liste de `GET /templates` du service, **verbatim** (statut et corps) : nom, version, sorties, schéma.\n\n### Sortie\n- **200** `application/pdf` — le binaire, avec les en-têtes du service relayés : `Content-Disposition`, `X-Render-Template`, `X-Render-Template-Version`, `X-Render-Paragraphes`, `X-Render-Commentaires`, `X-Render-Caracteres-Hebreu`, `X-Render-Duree-Ms`\n- **4xx/5xx** JSON `{ success, error, error_code }`\n\n### Dépendance\n`$env.GOTENBERG_API_URL` = `http://webs.local:3100`\n\n⚠️ `GOTENBERG_URL` (port 3000) est **obsolète** : Gotenberg n'est plus exposé.\n\nCode, templates, guide d'intégration : https://github.com/fsebbah/gotenberg.api\nContexte : azy.daily#139 · n8n-workflows#394",
        "height": 660,
        "width": 520
      },
      "id": "sticky-doc",
      "name": "Documentation",
      "type": "n8n-nodes-base.stickyNote",
      "typeVersion": 1,
      "position": [
        -180,
        -260
      ]
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "torah-pdf",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-torah-pdf",
      "name": "Webhook (torah-pdf)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        360
      ],
      "webhookId": "torah-pdf-webhook"
    },
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "render-pdf",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-render-pdf",
      "name": "Webhook (générique)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        160
      ],
      "webhookId": "render-pdf-webhook"
    },
    {
      "parameters": {
        "jsCode": "// Vérifie la configuration ET le payload AVANT tout appel réseau.\n// Objectif : qu'un oubli de configuration produise un message qui dit quoi faire,\n// et non un 404 opaque ou une URL « undefined/render/pdf ».\n\n// ⚠️ AUCUN « : » dans les messages levés ici. n8n ne garde, d'une erreur de Code\n// node, que ce qui suit le DERNIER deux-points, suffixé de « [line N] ». Mesuré le\n// 2026-09-10 : « payload invalide : un objet \"data\" est attendu » arrivait réduit\n// à « un objet \"data\" est attendu [line 24] », que Normalize Error ne savait plus\n// classer — un 400 sortait en 502. D'où « — » à la place de « : », et aucune URL.\nconst base = $env.GOTENBERG_API_URL;\nif (!base || !/^https?:\\/\\//.test(base)) {\n  throw new Error(\n    \"GOTENBERG_API_URL absent ou invalide dans l'environnement n8n. \" +\n    \"Ajouter GOTENBERG_API_URL (gotenberg.api, port 3100 sur webs.local) dans docker/.env.local, \" +\n    \"puis ./deploy.sh — éditer .env.local sans redéployer n'a aucun effet.\"\n  );\n}\n\nconst entree = $input.first().json;\nconst body = entree.body || entree;\nconst data = body.data || null;\n\n// Route empruntée : /render-pdf exige « template », /torah-pdf a un défaut\n// historique (le contrat déjà communiqué au plugin torah ne le mentionnait pas).\nconst chemin = String(entree.webhookUrl || entree.path || '');\nconst estGenerique = /render-pdf/.test(chemin);\n\nif (!data || typeof data !== 'object' || Array.isArray(data)) {\n  throw new Error('payload invalide — un objet \"data\" est attendu');\n}\nif (!body.template && estGenerique) {\n  throw new Error(\n    'champ requis manquant — \"template\" (obligatoire sur /webhook/render-pdf). ' +\n    'Liste des gabarits — GET /webhook/pdf-templates'\n  );\n}\n// Contrôles génériques uniquement : le schéma détaillé appartient au template,\n// et c'est gotenberg.api qui le connaît. Une seule source de vérité.\n\n\n// Le contrôle de fond (schéma détaillé, présence d'hébreu) est fait par\n// gotenberg.api, qui connaît le schéma du template. On ne le duplique pas ici :\n// une seule source de vérité pour la validation.\n\n// Format de sortie (azy.daily#355) : « pdf » par défaut, « epub » sur demande.\n// Un format inconnu est REFUSÉ, jamais ramené à « pdf » : un client qui demande\n// un EPUB et reçoit un PDF nommerait mal le fichier sans le savoir. Un format\n// connu mais non déclaré par le gabarit est refusé par le service lui-même\n// (400 UNSUPPORTED_OUTPUT), relayé tel quel.\nconst FORMATS = ['pdf', 'epub'];\nconst format = String(body.format || 'pdf').trim().toLowerCase();\nif (!FORMATS.includes(format)) {\n  throw new Error(\n    // la valeur reçue est citée sans ses « : » éventuels, pour la même raison\n    `payload invalide — format \"${String(body.format).slice(0, 20).replace(/:/g, ' ')}\" inconnu (formats ${FORMATS.join(', ')})`\n  );\n}\n\nreturn [{ json: {\n  url: base.replace(/\\/+$/, '') + '/render/' + format,\n  corps: {\n    template: body.template || (estGenerique ? null : 'bavli-visavis'),\n    data,\n    // Sur /render-pdf sans filename, le service nomme d'après le gabarit\n    // (« document.pdf »). Le repli massekhet_daf ne vaut que pour l'alias\n    // torah : ailleurs il donnait « undefined_undefined.pdf », invisible tant\n    // que Content-Disposition n'était pas relayé (azy.daily#355).\n    filename: body.filename || (estGenerique ? undefined : `${data.massekhet}_${data.daf}.${format}`),\n  },\n  correlation_id: body.correlation_id || null,\n} }];"
      },
      "id": "validate-input",
      "name": "Validate Input",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        300
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $json.url }}",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify($json.corps) }}",
        "options": {
          "response": {
            "response": {
              "responseFormat": "file",
              "outputPropertyName": "data",
              "fullResponse": true
            }
          },
          "timeout": 180000
        }
      },
      "id": "call-gotenberg-api",
      "name": "gotenberg.api",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        440,
        300
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "respondWith": "binary",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "={{ ($json.headers || {})['content-type'] || 'application/pdf' }}"
              },
              {
                "name": "Content-Disposition",
                "value": "={{ ($json.headers || {})['content-disposition'] || '' }}"
              },
              {
                "name": "X-Render-Template",
                "value": "={{ ($json.headers || {})['x-render-template'] || '' }}"
              },
              {
                "name": "X-Render-Template-Version",
                "value": "={{ ($json.headers || {})['x-render-template-version'] || '' }}"
              },
              {
                "name": "X-Render-Paragraphes",
                "value": "={{ ($json.headers || {})['x-render-paragraphes'] || '' }}"
              },
              {
                "name": "X-Render-Commentaires",
                "value": "={{ ($json.headers || {})['x-render-commentaires'] || '' }}"
              },
              {
                "name": "X-Render-Caracteres-Hebreu",
                "value": "={{ ($json.headers || {})['x-render-caracteres-hebreu'] || '' }}"
              },
              {
                "name": "X-Render-Duree-Ms",
                "value": "={{ ($json.headers || {})['x-render-duree-ms'] || '' }}"
              }
            ]
          }
        }
      },
      "id": "respond-pdf",
      "name": "Respond PDF",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        660,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Restitue à l'appelant l'erreur RÉELLE : message, code et statut HTTP d'origine.\n//\n// n8n emballe la réponse d'erreur d'un HTTP Request dans une chaîne de la forme\n//   404 - \"{\\\"success\\\":false,\\\"error\\\":\\\"...\\\",\\\"error_code\\\":\\\"UNKNOWN_TEMPLATE\\\"}\"\n// (JSON doublement échappé). Sans désencapsulation, on renverrait « erreur inconnue »\n// et un 502 générique — ce qui ferait conclure à tort à une panne, et réessayer.\n\nconst item = $input.first().json;\n\n// 1. Retrouver le message, quelle que soit la forme que n8n lui a donnée\nlet brut = '';\nfor (const c of [item?.error?.message, item?.message, item?.error?.description,\n                 item?.error?.error, typeof item?.error === 'string' ? item.error : null]) {\n  if (c) { brut = String(c); break; }\n}\nif (!brut) brut = 'erreur inconnue';\n// n8n suffixe l'erreur d'un Code node par « [line N] » : du bruit pour l'appelant.\nbrut = brut.replace(/\\s*\\[line \\d+\\]\\s*$/, '');\n\nlet message = brut;\nlet code = 'PDF_GENERATION_FAILED';\nlet statut = 502;   // défaut : la passerelle n'a pas pu joindre le service\n\n// 2. Statut HTTP émis par gotenberg.api, s'il est présent en tête du message\nconst mStatut = brut.match(/^\\s*(\\d{3})\\s*-\\s*/);\nif (mStatut) statut = parseInt(mStatut[1], 10);\n\n// 3. Charge utile JSON de gotenberg.api — d'abord telle quelle, sinon déséchappée\nconst extraire = (s) => {\n  const i = s.indexOf('{'), j = s.lastIndexOf('}');\n  if (i < 0 || j <= i) return null;\n  const bloc = s.slice(i, j + 1);\n  for (const essai of [bloc, bloc.replace(/\\\\\"/g, '\"').replace(/\\\\\\\\/g, '\\\\')]) {\n    try {\n      const o = JSON.parse(essai);\n      if (o && (o.error || o.error_code)) return o;\n    } catch (e) { /* forme suivante */ }\n  }\n  return null;\n};\nconst charge = extraire(brut);\nif (charge) {\n  if (charge.error) message = String(charge.error);\n  if (charge.error_code) code = String(charge.error_code);\n}\n\n// 4. Erreurs levées AVANT l'appel réseau (node Validate Input) : pas de statut ni de\n//    charge utile — ce sont des erreurs de requête ou de configuration.\nif (!mStatut && !charge) {\n  message = brut.replace(/^Error:\\s*/, '');\n  if (/GOTENBERG_API_URL/.test(message)) { code = 'CONFIG_MISSING'; statut = 500; }\n  // Même code que le refus du service pour un format non déclaré : un client\n  // n'a qu'un cas à traiter, qu'il vienne de n8n ou de gotenberg.api.\n  else if (/format \"[^\"]*\" inconnu/.test(message)) { code = 'UNSUPPORTED_OUTPUT'; statut = 400; }\n  else if (/champ requis|payload invalide|template/i.test(message)) { code = 'INVALID_DATA'; statut = 400; }\n}\n\nreturn [{ json: { success: false, error: message.slice(0, 500), error_code: code, http_status: statut } }];"
      },
      "id": "normalize-error",
      "name": "Normalize Error",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        440,
        500
      ]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": "={{ $json.http_status || 502 }}",
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              }
            ]
          }
        }
      },
      "id": "respond-error",
      "name": "Respond Error",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        660,
        500
      ]
    },
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "pdf-templates",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "5fca9aff-2672-418f-b505-360c1ccb203f",
      "name": "Webhook (catalogue)",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        0,
        580
      ],
      "webhookId": "46eb0fbb-d9da-4858-9d00-6ed70a708529"
    },
    {
      "parameters": {
        "jsCode": "// Catalogue des gabarits (azy.daily#355) : la liste de gotenberg.api, VERBATIM.\n// Le service fait foi — nom, version, sorties, schéma. n8n ne filtre, ne trie ni\n// ne reformate rien : une liste retouchée ici deviendrait une seconde vérité.\nconst base = $env.GOTENBERG_API_URL;\nif (!base || !/^https?:\\/\\//.test(base)) {\n  throw new Error(\n    \"GOTENBERG_API_URL absent ou invalide dans l'environnement n8n. \" +\n    \"Ajouter GOTENBERG_API_URL (gotenberg.api, port 3100 sur webs.local) dans docker/.env.local, puis ./deploy.sh.\"\n  );\n}\nreturn [{ json: { url: base.replace(/\\/+$/, '') + '/templates' } }];"
      },
      "id": "1614e88d-f2da-4fd0-81f5-86bb8d6de703",
      "name": "Préparer catalogue",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        580
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "method": "GET",
        "url": "={{ $json.url }}",
        "options": {
          "response": {
            "response": {
              "fullResponse": true,
              "neverError": true
            }
          },
          "timeout": 30000
        }
      },
      "id": "36b9b6f1-eb09-4e55-bf34-8c24e82835e4",
      "name": "Catalogue gotenberg.api",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        440,
        580
      ],
      "onError": "continueErrorOutput"
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json.body) }}",
        "options": {
          "responseCode": "={{ $json.statusCode || 502 }}",
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json"
              }
            ]
          }
        }
      },
      "id": "2a39e6ce-49f6-4313-90b4-def0d0994e69",
      "name": "Respond Catalogue",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [
        660,
        580
      ]
    }
  ],
  "connections": {
    "Validate Input": {
      "main": [
        [
          {
            "node": "gotenberg.api",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Normalize Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "gotenberg.api": {
      "main": [
        [
          {
            "node": "Respond PDF",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Normalize Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalize Error": {
      "main": [
        [
          {
            "node": "Respond Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook (torah-pdf)": {
      "main": [
        [
          {
            "node": "Validate Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook (générique)": {
      "main": [
        [
          {
            "node": "Validate Input",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Webhook (catalogue)": {
      "main": [
        [
          {
            "node": "Préparer catalogue",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Préparer catalogue": {
      "main": [
        [
          {
            "node": "Catalogue gotenberg.api",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Normalize Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Catalogue gotenberg.api": {
      "main": [
        [
          {
            "node": "Respond Catalogue",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Normalize Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "executionTimeout": 240,
    "saveDataSuccessExecution": "none",
    "saveExecutionProgress": false
  },
  "tags": []
}